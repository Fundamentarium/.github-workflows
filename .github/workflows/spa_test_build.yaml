name: SPA Test Build

on:
  pull_request:
    branches: [disabled]

jobs:
  spa-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run build
